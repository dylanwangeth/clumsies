openapi: 3.1.0
info:
  title: clumsies Local Daemon IPC Schema
  version: 1.0.0
  summary: Schema-only local daemon IPC contract for local daemon clients.
security: []
paths: {}
components:
  responses:
    Error:
      description: Error response.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    DaemonIpcTransport:
      type: string
      enum: [macos_xpc_mach_service]
    DaemonIpcEndpoint:
      type: object
      required: [transport, service_name]
      properties:
        transport:
          $ref: "#/components/schemas/DaemonIpcTransport"
        service_name:
          type: string
    DaemonIpcRequest:
      type: object
      required: [method, payload]
      properties:
        method:
          $ref: "#/components/schemas/DaemonIpcMethod"
        payload:
          type: object
          additionalProperties: true
        agent_runtime:
          $ref: "#/components/schemas/AgentRuntimeIdentity"
          description: Present only for short-lived Agent protocol proxies; Desktop and other resident clients omit it.
    DaemonIpcResponse:
      type: object
      required: [ok, payload, error]
      properties:
        ok:
          type: boolean
        payload:
          type: object
          additionalProperties: true
        error:
          oneOf:
            - $ref: "#/components/schemas/ApiError"
            - type: "null"
    DaemonIpcMethod:
      type: string
      enum:
        - health
        - project_config
        - replace_project_config
        - select_project
        - resolve_project_binding
        - list_project_bindings
        - replace_project_binding
        - remove_project_binding
        - list_project_agent_adapters
        - list_all_project_agent_adapters
        - inspect_legacy_agent_adapters
        - install_project_agent_adapter
        - remove_project_agent_adapter
        - sync_status
        - project_storage
        - replace_project_storage
        - project_storage_move
        - reset_project_storage
        - clear_project_cache
        - memory_cache
        - project_checkout
        - activate_memory
        - load_memory
        - record_agent_run_event
        - list_issue_board
        - desktop_list_issue_board
        - get_issue_detail
        - desktop_get_issue_detail
        - get_issue
        - export_issue
        - create_issue
        - update_issue
        - apply_issue_gate
        - set_verification_step_completed
        - unclaim_issue
        - desktop_unclaim_issue
        - remove_issue
        - start_issue_work
        - request_issue_closure
        - pause_issue
        - resume_issue
        - search_index_status
        - rebuild_search_index
        - list_retrieval_runs
        - get_retrieval_run
        - create_evaluation_case
        - resolve_evaluation_case
        - clear_retrieval_runs
        - export_evaluation_set
        - retry_sync
        - mcp_status
        - list_drafts
        - get_draft
        - store_draft_operation
        - desktop_store_draft_operation
        - server_request
    DaemonBootstrapStatus:
      type: object
      required:
        - label
        - mach_service_name
        - plist_path
        - installed
        - endpoint
        - runtime
      properties:
        label:
          type: string
        mach_service_name:
          type: string
        plist_path:
          type: string
        installed:
          type: boolean
        endpoint:
          $ref: "#/components/schemas/DaemonIpcEndpoint"
        runtime:
          $ref: "#/components/schemas/LaunchAgentRuntimeStatus"
    LaunchAgentRuntimeStatus:
      type: object
      required:
        - installed
        - bootstrapped
        - running
        - pid
        - state
        - last_exit_code
        - last_error
      properties:
        installed:
          type: boolean
        bootstrapped:
          type: boolean
        running:
          type: boolean
        pid:
          type: [integer, "null"]
        state:
          type: [string, "null"]
        last_exit_code:
          type: [integer, "null"]
        last_error:
          type: [string, "null"]
    DaemonProjectConfig:
      type: object
      required:
        - server_url
        - project_id
        - has_access_token
        - has_refresh_token
        - ready
        - missing_fields
      properties:
        server_url:
          type: string
          format: uri
        project_id:
          type: [string, "null"]
        has_access_token:
          type: boolean
          description: Whether the daemon loaded an access token from macOS Keychain.
        has_refresh_token:
          type: boolean
          description: Whether the daemon loaded a refresh token from macOS Keychain.
        ready:
          type: boolean
        missing_fields:
          type: array
          items:
            type: string
    DaemonProjectConfigUpdateRequest:
      type: object
      required: [server_url, project_id, access_token, refresh_token]
      properties:
        server_url:
          type: string
          format: uri
        project_id:
          type: [string, "null"]
        access_token:
          type: [string, "null"]
          description: Written directly to macOS Keychain and never returned by daemon APIs.
        refresh_token:
          type: [string, "null"]
          description: Written atomically with the access token to macOS Keychain and never returned.
    DaemonProjectSelectionRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    DaemonProjectBindingResolveRequest:
      type: object
      required: [workspace_path]
      properties:
        workspace_path:
          type: string
          description: Existing local path whose nearest bound ancestor should be resolved.
    DaemonProjectBindingListRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
          description: Canonical Server Project whose local repository bindings should be listed.
    DaemonProjectBindingListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/DaemonProjectBinding"
    DaemonProjectBindingReplaceRequest:
      type: object
      required: [workspace_root, project_id, expected_revision]
      properties:
        workspace_root:
          type: string
          description: Existing local directory to bind after canonical filesystem resolution.
        project_id:
          type: string
          description: Canonical Server Project identifier accessible to the signed-in user.
        expected_revision:
          type: [integer, "null"]
          minimum: 1
          description: Current binding revision for replacement; null creates a binding idempotently.
    DaemonProjectBindingRemoveRequest:
      type: object
      required: [workspace_root, expected_revision]
      properties:
        workspace_root:
          type: string
        expected_revision:
          type: integer
          minimum: 1
    DaemonProjectBindingRemoveResponse:
      type: object
      required: [workspace_root, removed]
      properties:
        workspace_root:
          type: string
        removed:
          type: boolean
    DaemonProjectBinding:
      type: object
      required:
        - server_url
        - workspace_root
        - project_id
        - revision
        - created_at
        - updated_at
      properties:
        server_url:
          type: string
          format: uri
        workspace_root:
          type: string
        project_id:
          type: string
        revision:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DaemonProjectAgentAdapterListRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    DaemonLegacyAgentAdapterInspectionRequest:
      type: object
      required: [runtime_binary_path]
      properties:
        runtime_binary_path:
          type: string
          description: Compatibility field retained for existing Apps and daemons; ignored by this read-only operation.
    DaemonLegacyAgentAdapterConflict:
      type: object
      required: [install_id, adapter, scope, target_root, code, message]
      properties:
        install_id:
          type: string
        adapter:
          $ref: "#/components/schemas/ProjectAgentAdapterKind"
        scope:
          type: string
          enum: [workspace, user, repo, unknown]
        target_root:
          type: string
        code:
          type: string
        message:
          type: string
    DaemonLegacyAgentAdapterInspectionResponse:
      type: object
      description: Read-only discovery result; archived Zig installations are never adopted implicitly.
      required: [scanned, deferred, conflicts]
      properties:
        scanned:
          type: integer
          minimum: 0
        deferred:
          type: integer
          minimum: 0
        conflicts:
          type: array
          items:
            $ref: "#/components/schemas/DaemonLegacyAgentAdapterConflict"
    DaemonProjectAgentAdapterInstallRequest:
      type: object
      required:
        - project_id
        - workspace_root
        - adapter
        - runtime_binary_path
        - expected_revision
      properties:
        project_id:
          type: string
        workspace_root:
          type: string
        adapter:
          $ref: "#/components/schemas/ProjectAgentAdapterKind"
        runtime_binary_path:
          type: string
          description: Signed App-bundled clumsiesd Agent runtime executable.
        expected_revision:
          type: [integer, "null"]
          minimum: 1
    DaemonProjectAgentAdapterRemoveRequest:
      type: object
      required: [workspace_root, adapter, expected_revision]
      properties:
        workspace_root:
          type: string
        adapter:
          $ref: "#/components/schemas/ProjectAgentAdapterKind"
        expected_revision:
          type: integer
          minimum: 1
    DaemonProjectAgentAdapterRemoveResponse:
      type: object
      required: [workspace_root, adapter, removed]
      properties:
        workspace_root:
          type: string
        adapter:
          $ref: "#/components/schemas/ProjectAgentAdapterKind"
        removed:
          type: boolean
    DaemonProjectAgentAdapter:
      type: object
      required:
        - server_url
        - project_id
        - workspace_root
        - adapter
        - revision
        - managed_files
        - created_at
        - updated_at
      properties:
        server_url:
          type: string
          format: uri
        project_id:
          type: string
        workspace_root:
          type: string
        adapter:
          $ref: "#/components/schemas/ProjectAgentAdapterKind"
        revision:
          type: integer
          minimum: 1
        managed_files:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DaemonProjectAgentAdapterListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/DaemonProjectAgentAdapter"
    DaemonProjectStorageRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    DaemonProjectStorageMoveRequest:
      type: object
      required: [move_id]
      properties:
        move_id:
          type: string
    DaemonProjectStorageReplaceRequest:
      type: object
      required: [project_id, selected_root_path, handoff_bookmark_data, expected_location_revision]
      properties:
        project_id:
          type: string
        selected_root_path:
          type: string
          description: Canonical display path selected by the user; daemon validates it against the handoff bookmark.
        handoff_bookmark_data:
          type: string
          format: byte
          description: Base64-encoded one-time macOS bookmark used by Desktop to hand the selected directory to daemon. Daemon replaces it with its own security-scoped bookmark and never persists this value.
        expected_location_revision:
          type: integer
          minimum: 1
    DaemonProjectStorageResetRequest:
      type: object
      required: [project_id, expected_location_revision]
      properties:
        project_id:
          type: string
        expected_location_revision:
          type: integer
          minimum: 1
    DaemonProjectCacheClearRequest:
      type: object
      required: [project_id, expected_location_revision]
      properties:
        project_id:
          type: string
        expected_location_revision:
          type: integer
          minimum: 1
    DaemonProjectStorage:
      type: object
      required:
        - authority_key
        - project_id
        - mode
        - selected_root_path
        - managed_root_path
        - active_generation_path
        - search_index_path
        - availability
        - location_revision
        - size_bytes
        - active_move_id
        - issue_code
        - diagnostic
      properties:
        authority_key:
          type: string
          format: uri
        project_id:
          type: string
        mode:
          $ref: "#/components/schemas/DaemonProjectStorageMode"
        selected_root_path:
          type: string
        managed_root_path:
          type: string
        active_generation_path:
          type: [string, "null"]
        search_index_path:
          type: string
        availability:
          $ref: "#/components/schemas/DaemonProjectStorageAvailability"
        location_revision:
          type: integer
          minimum: 1
        size_bytes:
          type: integer
          minimum: 0
        active_move_id:
          type: [string, "null"]
        issue_code:
          type: [string, "null"]
        diagnostic:
          type: [string, "null"]
    DaemonProjectStorageMove:
      type: object
      required:
        - move_id
        - project_id
        - source_mode
        - destination_mode
        - source_managed_root_path
        - destination_managed_root_path
        - source_location_revision
        - state
        - error_code
        - error_message
        - created_at
        - updated_at
        - completed_at
      properties:
        move_id:
          type: string
        project_id:
          type: string
        source_mode:
          $ref: "#/components/schemas/DaemonProjectStorageMode"
        destination_mode:
          $ref: "#/components/schemas/DaemonProjectStorageMode"
        source_managed_root_path:
          type: string
        destination_managed_root_path:
          type: string
        source_location_revision:
          type: integer
          minimum: 1
        state:
          $ref: "#/components/schemas/DaemonProjectStorageMoveState"
        error_code:
          type: [string, "null"]
        error_message:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type: [string, "null"]
          format: date-time
    DaemonMemoryCacheRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    DaemonMemoryCacheStatus:
      type: object
      required: [project_id, commit_id, active_generation_path, state, diagnostic]
      properties:
        project_id:
          type: string
        commit_id:
          type: [string, "null"]
        active_generation_path:
          type: [string, "null"]
        state:
          $ref: "#/components/schemas/DaemonMemoryCacheState"
        diagnostic:
          type: [string, "null"]
    DaemonProjectCheckoutRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    DaemonProjectCheckout:
      type: object
      required:
        - project_id
        - commit_id
        - ref_etag
        - commit_created_at
        - org_selection_revision
        - selected_org_resource_ids
        - resources
        - ready
      properties:
        project_id:
          type: string
        commit_id:
          type: [string, "null"]
        ref_etag:
          type: [string, "null"]
        commit_created_at:
          type: [string, "null"]
          format: date-time
        org_selection_revision:
          type: integer
        selected_org_resource_ids:
          type: array
          items:
            type: string
        resources:
          type: array
          items:
            $ref: "#/components/schemas/DaemonProjectCheckoutResource"
        ready:
          type: boolean
    DaemonProjectCheckoutResource:
      type: object
      required:
        - resource_id
        - scope
        - resource_kind
        - project_id
        - path
        - content_hash
        - content
      properties:
        resource_id:
          type: string
        scope:
          $ref: "#/components/schemas/DaemonDraftScope"
        resource_kind:
          $ref: "#/components/schemas/DaemonDraftResourceKind"
        project_id:
          type: [string, "null"]
        path:
          type: string
        content_hash:
          type: string
        content:
          $ref: "#/components/schemas/DaemonDraftContent"
    RecordAgentRunEventRequest:
      type: object
      required:
        - event_id
        - project_id
        - host
        - host_run_key
        - event_type
        - source
        - host_session_id
        - parent_run_id
        - parent_host_run_key
        - kind
        - issue_key
        - outcome
        - display_label
        - summary
        - occurred_at
      properties:
        event_id:
          type: string
        project_id:
          type: string
        host:
          $ref: "#/components/schemas/AgentRunHost"
        host_run_key:
          type: [string, "null"]
          description: Null only for a host-session-wide event such as session_ended.
        event_type:
          $ref: "#/components/schemas/AgentRunEventType"
        source:
          $ref: "#/components/schemas/AgentRunEventSource"
        host_session_id:
          type: [string, "null"]
        parent_run_id:
          type: [string, "null"]
        parent_host_run_key:
          type: [string, "null"]
          description: Host key used to resolve a parent run before its daemon run id is known.
        kind:
          oneOf:
            - $ref: "#/components/schemas/AgentRunKind"
            - type: "null"
        issue_key:
          type: [string, "null"]
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        outcome:
          oneOf:
            - $ref: "#/components/schemas/AgentRunOutcome"
            - type: "null"
        display_label:
          type: [string, "null"]
          maxLength: 160
        summary:
          type: [string, "null"]
          maxLength: 1000
        occurred_at:
          type: [string, "null"]
          format: date-time
    RecordAgentRunEventResponse:
      type: object
      required: [run, affected_runs, duplicate]
      properties:
        run:
          description: Null for a host-session-wide event that affects multiple runs.
          oneOf:
            - $ref: "#/components/schemas/AgentRun"
            - type: "null"
        affected_runs:
          type: array
          items:
            $ref: "#/components/schemas/AgentRun"
        duplicate:
          type: boolean
    IssueBoardListRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    IssueDetailRequest:
      type: object
      required: [project_id, issue_number]
      properties:
        project_id:
          type: string
        issue_number:
          type: integer
          minimum: 1
          maximum: 999
    GetIssueRequest:
      type: object
      required: [issue_id]
      properties:
        issue_id:
          type: string
          pattern: "^issue_[0-9a-f]{32}$"
          description: Globally unique Issue identifier, independent of project scope.
    IssueDetailResponse:
      type: object
      required: [issue, body, acceptance_criteria]
      properties:
        issue:
          $ref: "#/components/schemas/IssueBoardCard"
        body:
          type: string
        acceptance_criteria:
          type: array
          items:
            type: string
    IssueBoardResponse:
      type: object
      required: [project_id, effective_hash, issues, claims, unlinked_runs, diagnostics]
      properties:
        project_id:
          type: string
        effective_hash:
          type: string
        issues:
          type: array
          items:
            $ref: "#/components/schemas/IssueBoardCard"
        claims:
          type: array
          items:
            $ref: "#/components/schemas/IssueClaim"
        unlinked_runs:
          type: array
          items:
            $ref: "#/components/schemas/AgentRun"
        diagnostics:
          type: array
          items:
            $ref: "#/components/schemas/IssueBoardDiagnostic"
    IssueMember:
      type: object
      required: [user_id, email, display_name, avatar_url, role]
      properties:
        user_id:
          type: string
        email:
          type: string
          format: email
        display_name:
          type: [string, "null"]
        avatar_url:
          type: [string, "null"]
          format: uri
        role:
          type: string
    IssueClaim:
      type: object
      required:
        - project_id
        - issue_id
        - issue_key
        - run_id
        - claimant
        - claimed_at
        - lease_expires_at
      properties:
        project_id:
          type: string
        issue_id:
          type: string
        issue_key:
          type: string
        run_id:
          type: string
        claimant:
          $ref: "#/components/schemas/IssueMember"
        claimed_at:
          type: string
          format: date-time
        lease_expires_at:
          type: string
          format: date-time
    IssueExternalReference:
      type: object
      required: [kind, url]
      properties:
        kind:
          $ref: "#/components/schemas/IssueExternalReferenceKind"
        url:
          type: string
          format: uri
          minLength: 1
          maxLength: 2048
          description: Absolute HTTP(S) URL without embedded credentials.
    IssueBlockingFactKind:
      type: string
      enum: [host_capability, external]
    IssueBlockingFact:
      type: object
      required: [fact_id, kind, description, satisfied]
      properties:
        fact_id:
          type: string
        kind:
          $ref: "#/components/schemas/IssueBlockingFactKind"
        value:
          type: [string, "null"]
        description:
          type: string
        satisfied:
          type: boolean
    IssueBlockingReasonKind:
      type: string
      enum: [dependency, fact]
    IssueBlockingReason:
      type: object
      required: [kind]
      properties:
        kind:
          $ref: "#/components/schemas/IssueBlockingReasonKind"
        issue_key:
          type: [string, "null"]
        title:
          type: [string, "null"]
        board_state:
          oneOf:
            - $ref: "#/components/schemas/IssueBoardState"
            - type: "null"
        fact_id:
          type: [string, "null"]
        description:
          type: [string, "null"]
    IssueDependencyState:
      type: object
      required: [issue_key, title, board_state]
      properties:
        issue_key:
          type: string
        title:
          type: string
        board_state:
          $ref: "#/components/schemas/IssueBoardState"
    IssueBoardCard:
      type: object
      required:
        - issue_id
        - project_id
        - issue_number
        - issue_key
        - resource_id
        - path
        - lifecycle
        - title
        - description
        - external_references
        - found_at
        - created_at
        - started_at
        - closed_at
        - archived_at
        - content_hash
        - source_commit_id
        - draft_id
        - draft_revision
        - board_state
        - state_revision
        - state_updated_at
        - closure_summary
        - assignee
        - is_stale
        - blocked
        - blocking_reasons
        - dependencies
        - blocking_facts
        - active_runs
        - latest_run
        - changed_by_run_id
        - verification_level
        - verification_steps
        - state_events
      properties:
        issue_id:
          type: string
          pattern: "^issue_[0-9a-f]{32}$"
        project_id:
          type: string
        issue_number:
          type: integer
          minimum: 1
        issue_key:
          type: string
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        resource_id:
          type: string
        path:
          type: string
        lifecycle:
          $ref: "#/components/schemas/IssueLifecycle"
        title:
          type: string
        description:
          type: string
        description_excerpt:
          type: string
        external_references:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/IssueExternalReference"
        found_at:
          type: [string, "null"]
          format: date-time
        created_at:
          type: [string, "null"]
          format: date-time
        started_at:
          type: [string, "null"]
          format: date-time
        closed_at:
          type: [string, "null"]
          format: date-time
        archived_at:
          type: [string, "null"]
          format: date-time
        content_hash:
          type: string
        source_commit_id:
          type: [string, "null"]
        draft_id:
          type: [string, "null"]
        draft_revision:
          type: [string, "null"]
        board_state:
          $ref: "#/components/schemas/IssueBoardState"
        state_revision:
          type: integer
          minimum: 1
        state_updated_at:
          type: [string, "null"]
          format: date-time
        closure_summary:
          type: [string, "null"]
          maxLength: 1000
        assignee:
          oneOf:
            - $ref: "#/components/schemas/IssueMember"
            - type: "null"
        is_stale:
          type: boolean
        blocked:
          type: boolean
        blocking_reasons:
          type: array
          items:
            $ref: "#/components/schemas/IssueBlockingReason"
        dependencies:
          type: array
          items:
            $ref: "#/components/schemas/IssueDependencyState"
        blocking_facts:
          type: array
          items:
            $ref: "#/components/schemas/IssueBlockingFact"
        active_runs:
          type: array
          items:
            $ref: "#/components/schemas/AgentRun"
        latest_run:
          oneOf:
            - $ref: "#/components/schemas/AgentRun"
            - type: "null"
        changed_by_run_id:
          type: [string, "null"]
        verification_level:
          $ref: "#/components/schemas/VerificationLevel"
        verification_steps:
          type: array
          items:
            $ref: "#/components/schemas/VerificationStep"
        state_events:
          type: array
          items:
            $ref: "#/components/schemas/IssueStateEvent"
    IssueBoardDiagnostic:
      type: object
      required: [resource_id, path, code, message]
      properties:
        resource_id:
          type: string
        path:
          type: string
        code:
          $ref: "#/components/schemas/IssueBoardDiagnosticCode"
        message:
          type: string
    AgentRun:
      type: object
      required:
        - run_id
        - project_id
        - issue_number
        - host
        - host_run_key
        - host_session_id
        - parent_run_id
        - kind
        - phase
        - outcome
        - end_reason
        - display_label
        - summary
        - revision
        - started_at
        - last_seen_at
        - lease_expires_at
        - ended_at
      properties:
        run_id:
          type: string
        project_id:
          type: string
        issue_number:
          type: [integer, "null"]
          minimum: 1
        host:
          $ref: "#/components/schemas/AgentRunHost"
        host_run_key:
          type: string
        host_session_id:
          type: [string, "null"]
        parent_run_id:
          type: [string, "null"]
        kind:
          $ref: "#/components/schemas/AgentRunKind"
        phase:
          $ref: "#/components/schemas/AgentRunPhase"
        outcome:
          oneOf:
            - $ref: "#/components/schemas/AgentRunOutcome"
            - type: "null"
        end_reason:
          type: [string, "null"]
        display_label:
          type: [string, "null"]
        summary:
          type: [string, "null"]
          maxLength: 1000
        revision:
          type: integer
          minimum: 1
        started_at:
          type: string
          format: date-time
        last_seen_at:
          type: string
          format: date-time
        lease_expires_at:
          type: string
          format: date-time
        ended_at:
          type: [string, "null"]
          format: date-time
    CreateIssueRequest:
      type: object
      required: [project_id, title, description]
      properties:
        project_id:
          type: string
        title:
          type: string
          minLength: 1
          maxLength: 240
        description:
          type: string
          minLength: 1
          maxLength: 65536
        acceptance_criteria:
          type: array
          maxItems: 64
          items:
            type: string
        external_references:
          type: array
          maxItems: 16
          items:
            $ref: "#/components/schemas/IssueExternalReference"
    UpdateIssueRequest:
      type: object
      required: [project_id, issue_key, expected_revision]
      properties:
        project_id:
          type: string
        issue_key:
          type: string
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        expected_revision:
          type: integer
          minimum: 1
        title:
          type: [string, "null"]
        description:
          type: [string, "null"]
        acceptance_criteria:
          type: [array, "null"]
          maxItems: 64
          items:
            type: string
        external_references:
          type: [array, "null"]
          maxItems: 16
          items:
            $ref: "#/components/schemas/IssueExternalReference"
    ApplyIssueGateRequest:
      type: object
      required: [project_id, issue_number, expected_revision, action]
      properties:
        project_id:
          type: string
        issue_number:
          type: integer
          minimum: 1
          maximum: 999
        expected_revision:
          type: integer
          minimum: 1
        action:
          $ref: "#/components/schemas/IssueGateAction"
    IssueMutationResponse:
      type: object
      required: [issue_id, issue_key, board_state, revision, updated_at]
      properties:
        issue_id:
          type: string
          pattern: "^issue_[0-9a-f]{32}$"
        issue_key:
          type: string
        board_state:
          $ref: "#/components/schemas/IssueBoardState"
        revision:
          type: integer
          minimum: 1
        updated_at:
          type: string
          format: date-time
    RemoveIssueRequest:
      type: object
      required: [project_id, issue_number, expected_revision, action]
      properties:
        project_id:
          type: string
        issue_number:
          type: integer
          minimum: 1
          maximum: 999
        expected_revision:
          type: integer
          minimum: 1
        action:
          $ref: "#/components/schemas/IssueRemovalAction"
    IssueRemovalResponse:
      type: object
      required: [issue_id, issue_key, action, removed_at]
      properties:
        issue_id:
          type: string
          pattern: "^issue_[0-9a-f]{32}$"
        issue_key:
          type: string
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        action:
          $ref: "#/components/schemas/IssueRemovalAction"
        removed_at:
          type: string
          format: date-time
    StartIssueWorkRequest:
      type: object
      required: [project_id, run_id, issue_key, expected_revision]
      properties:
        project_id:
          type: string
        run_id:
          type: string
        issue_key:
          type: string
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        expected_revision:
          type: integer
          minimum: 1
    RequestIssueClosureRequest:
      type: object
      required: [project_id, run_id, summary, expected_revision]
      properties:
        project_id:
          type: string
        run_id:
          type: string
        summary:
          type: [string, "null"]
          maxLength: 1000
          description: At most 1000 UTF-8 bytes at the daemon boundary.
        expected_revision:
          type: integer
          minimum: 1
    IssueWorkflowMutationResponse:
      type: object
      required: [issue_id, issue_key, board_state, state_revision, state_updated_at, run]
      properties:
        issue_id:
          type: string
          pattern: "^issue_[0-9a-f]{32}$"
        issue_key:
          type: string
          pattern: "^ISSUE-(?!000$)[0-9]{3}$"
        board_state:
          $ref: "#/components/schemas/IssueBoardState"
        state_revision:
          type: integer
          minimum: 1
        state_updated_at:
          type: string
          format: date-time
        run:
          $ref: "#/components/schemas/AgentRun"
    RetrievalRunListRequest:
      type: object
      properties:
        project_id:
          type: [string, "null"]
        status:
          oneOf:
            - $ref: "#/components/schemas/RetrievalRunStatus"
            - type: "null"
        cursor:
          type: [string, "null"]
        limit:
          type: [integer, "null"]
          minimum: 1
          maximum: 100
    RetrievalRunRequest:
      type: object
      required: [run_id]
      properties:
        run_id:
          type: string
    RetrievalRunListResponse:
      type: object
      required: [items, next_cursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RetrievalRun"
        next_cursor:
          type: [string, "null"]
    RetrievalRun:
      type: object
      required:
        - run_id
        - project_id
        - query
        - activation_state_fingerprint
        - status
        - effective_hash
        - index_revision
        - resource_count
        - unit_count
        - parser_version
        - chunker_version
        - model_revision
        - ranking_profile
        - latencies
        - returned_fragment_count
        - returned_token_count
        - error_stage
        - error_code
        - error_summary
        - created_at
        - completed_at
        - evaluation_case_id
        - evaluation_case_status
      properties:
        run_id:
          type: string
        project_id:
          type: string
        query:
          type: string
        activation_state_fingerprint:
          type: string
        status:
          $ref: "#/components/schemas/RetrievalRunStatus"
        effective_hash:
          type: [string, "null"]
        index_revision:
          type: [string, "null"]
        resource_count:
          type: integer
          minimum: 0
        unit_count:
          type: integer
          minimum: 0
        parser_version:
          type: [string, "null"]
        chunker_version:
          type: [string, "null"]
        model_revision:
          type: [string, "null"]
        ranking_profile:
          type: [string, "null"]
        latencies:
          $ref: "#/components/schemas/RetrievalStageLatencies"
        returned_fragment_count:
          type: integer
          minimum: 0
        returned_token_count:
          type: integer
          minimum: 0
        error_stage:
          type: [string, "null"]
        error_code:
          type: [string, "null"]
        error_summary:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
        completed_at:
          type: [string, "null"]
          format: date-time
        evaluation_case_id:
          type: [string, "null"]
        evaluation_case_status:
          oneOf:
            - $ref: "#/components/schemas/EvaluationCaseStatus"
            - type: "null"
    RetrievalStageLatencies:
      type: object
      required:
        - effective_memory_us
        - index_ensure_us
        - bm25_us
        - embedding_us
        - vector_us
        - rrf_us
        - rerank_us
        - assembly_us
        - persistence_us
        - total_us
      properties:
        effective_memory_us:
          type: integer
          minimum: 0
        index_ensure_us:
          type: integer
          minimum: 0
        bm25_us:
          type: integer
          minimum: 0
        embedding_us:
          type: integer
          minimum: 0
        vector_us:
          type: integer
          minimum: 0
        rrf_us:
          type: integer
          minimum: 0
        rerank_us:
          type: integer
          minimum: 0
        assembly_us:
          type: integer
          minimum: 0
        persistence_us:
          type: integer
          minimum: 0
        total_us:
          type: integer
          minimum: 0
    RetrievalCandidate:
      type: object
      required:
        - unit_key
        - resource_id
        - scope
        - kind
        - path
        - heading_path
        - locator
        - content_hash
        - resource_content_hash
        - token_count
        - evidence_excerpt
        - exact_rank
        - bm25_rank
        - bm25_score
        - vector_rank
        - vector_score
        - rrf_rank
        - rrf_score
        - reranker_rank
        - reranker_logit
        - reranker_relevance
        - final_rank
        - selected
        - exclusion_reason
        - delta_action
      properties:
        unit_key:
          type: string
        resource_id:
          type: string
        scope:
          $ref: "#/components/schemas/DaemonDraftScope"
        kind:
          $ref: "#/components/schemas/DaemonDraftResourceKind"
        path:
          type: string
        heading_path:
          type: array
          items:
            type: string
        locator:
          $ref: "#/components/schemas/RetrievalSourceLocator"
        content_hash:
          type: string
        resource_content_hash:
          type: string
        token_count:
          type: integer
          minimum: 0
        evidence_excerpt:
          type: string
        exact_rank:
          type: [integer, "null"]
          minimum: 1
        bm25_rank:
          type: [integer, "null"]
          minimum: 1
        bm25_score:
          type: [number, "null"]
        vector_rank:
          type: [integer, "null"]
          minimum: 1
        vector_score:
          type: [number, "null"]
        rrf_rank:
          type: [integer, "null"]
          minimum: 1
        rrf_score:
          type: [number, "null"]
        reranker_rank:
          type: [integer, "null"]
          minimum: 1
        reranker_logit:
          type: [number, "null"]
        reranker_relevance:
          type: [number, "null"]
        final_rank:
          type: [integer, "null"]
          minimum: 1
        selected:
          type: boolean
        exclusion_reason:
          $ref: "#/components/schemas/RetrievalExclusionReason"
        delta_action:
          oneOf:
            - $ref: "#/components/schemas/RetrievalDeltaAction"
            - type: "null"
    RetrievalSourceLocator:
      type: object
      required: [type, start_byte, end_byte, heading_path]
      properties:
        type:
          type: string
          enum: [markdown_span]
        start_byte:
          type: integer
          minimum: 0
        end_byte:
          type: integer
          minimum: 0
        heading_path:
          type: array
          items:
            type: string
    RetrievalRunDetail:
      type: object
      required:
        [run, candidates, evaluation_case, evidence, evidence_suggestions, report]
      properties:
        run:
          $ref: "#/components/schemas/RetrievalRun"
        candidates:
          type: array
          items:
            $ref: "#/components/schemas/RetrievalCandidate"
        evaluation_case:
          oneOf:
            - $ref: "#/components/schemas/EvaluationCase"
            - type: "null"
        evidence:
          type: array
          items:
            $ref: "#/components/schemas/EvaluationEvidence"
        evidence_suggestions:
          type: array
          items:
            $ref: "#/components/schemas/EvaluationEvidenceSuggestion"
        report:
          oneOf:
            - $ref: "#/components/schemas/RetrievalBenchmarkReport"
            - type: "null"
    CreateEvaluationCaseRequest:
      type: object
      required: [run_id]
      properties:
        run_id:
          type: string
    ResolveEvaluationCaseRequest:
      type: object
      required: [case_id, expected_version, evidence]
      properties:
        case_id:
          type: string
        expected_version:
          type: integer
          minimum: 1
        evidence:
          type: array
          items:
            $ref: "#/components/schemas/EvaluationEvidenceInput"
        none_matched:
          type: boolean
          default: false
    EvaluationEvidenceInput:
      type: object
      required: [resource_id]
      properties:
        resource_id:
          type: string
        unit_key:
          type: [string, "null"]
    EvaluationCaseStatus:
      type: string
      enum: [draft, needs_evidence, ready]
    EvaluationCase:
      type: object
      required:
        - case_id
        - source_run_id
        - corpus_id
        - project_id
        - query
        - status
        - version
        - created_at
        - updated_at
      properties:
        case_id:
          type: string
        source_run_id:
          type: string
        corpus_id:
          type: string
        project_id:
          type: string
        query:
          type: string
        status:
          $ref: "#/components/schemas/EvaluationCaseStatus"
        version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EvaluationEvidence:
      type: object
      required:
        - evidence_id
        - case_id
        - resource_id
        - unit_key
        - evidence_excerpt
      properties:
        evidence_id:
          type: string
        case_id:
          type: string
        resource_id:
          type: string
        unit_key:
          type: [string, "null"]
        evidence_excerpt:
          type: string
    RetrievalFailureStage:
      type: string
      enum: [fusion, reranking, assembly]
    EvaluationEvidenceSuggestion:
      type: object
      required:
        - resource_id
        - unit_key
        - path
        - heading_path
        - evidence_excerpt
        - model_relevance
        - likely_failure_stage
        - exclusion_reason
      properties:
        resource_id:
          type: string
        unit_key:
          type: string
        path:
          type: string
        heading_path:
          type: array
          items:
            type: string
        evidence_excerpt:
          type: string
        model_relevance:
          type: [number, "null"]
        likely_failure_stage:
          $ref: "#/components/schemas/RetrievalFailureStage"
        exclusion_reason:
          $ref: "#/components/schemas/RetrievalExclusionReason"
    EvaluationCaseDetail:
      type: object
      required: [evaluation_case, evidence, evidence_suggestions, report]
      properties:
        evaluation_case:
          $ref: "#/components/schemas/EvaluationCase"
        evidence:
          type: array
          items:
            $ref: "#/components/schemas/EvaluationEvidence"
        evidence_suggestions:
          type: array
          items:
            $ref: "#/components/schemas/EvaluationEvidenceSuggestion"
        report:
          oneOf:
            - $ref: "#/components/schemas/RetrievalBenchmarkReport"
            - type: "null"
    ClearRetrievalRunsRequest:
      type: object
      properties:
        project_id:
          type: [string, "null"]
    ClearRetrievalRunsResponse:
      type: object
      required: [deleted_run_count]
      properties:
        deleted_run_count:
          type: integer
          minimum: 0
    ExportEvaluationSetRequest:
      type: object
      properties:
        project_id:
          type: [string, "null"]
        case_ids:
          type: array
          items:
            type: string
          default: []
    ExportEvaluationSetResponse:
      type: object
      required: [fixture_json, report]
      properties:
        fixture_json:
          type: string
        report:
          $ref: "#/components/schemas/RetrievalBenchmarkReport"
    RetrievalBenchmarkReport:
      type: object
      required: [variants]
      properties:
        variants:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/RetrievalBenchmarkMetrics"
    RetrievalBenchmarkMetrics:
      type: object
      required:
        - case_count
        - recall_at_20
        - ndcg_at_10
        - mrr
        - resource_diversity
        - scope_violation
        - stale_result
        - warm_p50_us
        - warm_p95_us
      properties:
        case_count:
          type: integer
          minimum: 0
        recall_at_20:
          type: number
        ndcg_at_10:
          type: number
        mrr:
          type: number
        resource_diversity:
          type: number
        scope_violation:
          type: number
        stale_result:
          type: number
        warm_p50_us:
          type: integer
          minimum: 0
        warm_p95_us:
          type: integer
          minimum: 0
    DaemonServerRequest:
      type: object
      required: [method, path, headers, body]
      properties:
        method:
          type: string
          enum: [GET, POST, PUT, PATCH, DELETE]
        path:
          type: string
        headers:
          type: object
          additionalProperties:
            type: string
        body:
          type: [string, "null"]
    DaemonServerResponse:
      type: object
      required: [status, headers, body]
      properties:
        status:
          type: integer
          minimum: 100
          maximum: 599
        headers:
          type: object
          additionalProperties:
            type: string
        body:
          type: string
    DaemonHealth:
      type: object
      required:
        - daemon_version
        - agent_runtime
        - server_url
        - project_id
        - daemon_installation_id
        - log_dir
        - local_db
      properties:
        daemon_version:
          type: string
        agent_runtime:
          $ref: "#/components/schemas/AgentRuntimeIdentity"
        server_url:
          type: string
        project_id:
          type: [string, "null"]
        daemon_installation_id:
          type: string
        log_dir:
          type: string
        local_db:
          $ref: "#/components/schemas/LocalDbStatus"
    AgentRuntimeIdentity:
      type: object
      required: [protocol_revision, build_id]
      properties:
        protocol_revision:
          type: integer
          minimum: 1
        build_id:
          type: string
          minLength: 1
          maxLength: 128
    LocalDbStatus:
      type: object
      required: [path, ready, schema_version]
      properties:
        path:
          type: string
        ready:
          type: boolean
        schema_version:
          type: integer
    DaemonSyncStatus:
      type: object
      required:
        - draft_sync
        - commit_sync
        - pending_operation_count
        - failed_operation_count
        - behind_draft_count
        - reconciliation_conflict_count
        - last_success_at
      properties:
        draft_sync:
          $ref: "#/components/schemas/SyncChannelStatus"
        commit_sync:
          $ref: "#/components/schemas/SyncChannelStatus"
        pending_operation_count:
          type: integer
        failed_operation_count:
          type: integer
        behind_draft_count:
          type: integer
        reconciliation_conflict_count:
          type: integer
        last_success_at:
          type: [string, "null"]
          format: date-time
    SyncChannelStatus:
      type: object
      required: [state, server_cursor, last_attempt_at, last_success_at, last_error]
      properties:
        state:
          $ref: "#/components/schemas/SyncState"
        server_cursor:
          type: [string, "null"]
        last_attempt_at:
          type: [string, "null"]
          format: date-time
        last_success_at:
          type: [string, "null"]
          format: date-time
        last_error:
          oneOf:
            - $ref: "#/components/schemas/ApiError"
            - type: "null"
    DaemonSyncRetryRequest:
      type: object
      required: [channel]
      properties:
        channel:
          type: string
          enum: [drafts, commits, all]
    DaemonRetryResponse:
      type: object
      required: [retry_id, started]
      properties:
        retry_id:
          type: string
        started:
          type: boolean
    DaemonMcpStatus:
      type: object
      required: [running, endpoint, adapters]
      properties:
        running:
          type: boolean
        endpoint:
          type: [string, "null"]
        adapters:
          type: array
          items:
            $ref: "#/components/schemas/McpAdapterStatus"
    McpAdapterStatus:
      type: object
      required: [name, running, last_error]
      properties:
        name:
          type: string
        running:
          type: boolean
        last_error:
          oneOf:
            - $ref: "#/components/schemas/ApiError"
            - type: "null"
    DaemonDraftListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/DaemonDraftSummary"
    DaemonDraftDetail:
      type: object
      required: [draft, operations]
      properties:
        draft:
          $ref: "#/components/schemas/DaemonDraftSummary"
        operations:
          type: array
          items:
            $ref: "#/components/schemas/DaemonLocalDraftOperation"
    DaemonDraftSummary:
      type: object
      required:
        - draft_id
        - project_id
        - server_draft_id
        - server_version
        - base_commit_id
        - current_commit_id
        - freshness
        - has_upstream_resource_changes
        - reconciliation
        - reconciliation_candidate_id
        - scope
        - resource_kind
        - target_id
        - path
        - status
        - created_at
        - updated_at
        - pending_operation_count
        - failed_operation_count
      properties:
        draft_id:
          type: string
        project_id:
          type: string
        server_draft_id:
          type: [string, "null"]
        server_version:
          type: integer
        base_commit_id:
          type: [string, "null"]
        current_commit_id:
          type: [string, "null"]
        freshness:
          $ref: "#/components/schemas/DaemonDraftFreshness"
        has_upstream_resource_changes:
          type: boolean
          description: Whether the Draft resource changed between its Base Commit and current target Ref.
        reconciliation:
          $ref: "#/components/schemas/DaemonDraftReconciliationStatus"
        reconciliation_candidate_id:
          type: [string, "null"]
        scope:
          $ref: "#/components/schemas/DaemonDraftScope"
        resource_kind:
          $ref: "#/components/schemas/DaemonDraftResourceKind"
        target_id:
          type: [string, "null"]
        path:
          type: [string, "null"]
        status:
          $ref: "#/components/schemas/DaemonLocalDraftStatus"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        pending_operation_count:
          type: integer
        failed_operation_count:
          type: integer
    DaemonLocalDraftOperation:
      type: object
      required:
        - local_operation_id
        - resource_kind
        - operation
        - source
        - sync_status
        - last_error
        - created_at
        - updated_at
      properties:
        local_operation_id:
          type: string
        resource_kind:
          $ref: "#/components/schemas/DaemonDraftResourceKind"
        operation:
          $ref: "#/components/schemas/DaemonDraftOperation"
        source:
          $ref: "#/components/schemas/DaemonDraftOperationRecordSource"
        sync_status:
          $ref: "#/components/schemas/DraftOperationSyncStatus"
        last_error:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DaemonDraftOperationRequest:
      type: object
      required: [project_id, scope, resource, op]
      properties:
        draft_id:
          type: [string, "null"]
        base_commit_id:
          type: [string, "null"]
        project_id:
          type: string
        scope:
          $ref: "#/components/schemas/DaemonDraftScope"
        resource:
          $ref: "#/components/schemas/DaemonDraftResourceKind"
        op:
          $ref: "#/components/schemas/DaemonDraftOperation"
        source:
          $ref: "#/components/schemas/DaemonDraftOperationSource"
    DaemonDraftOperation:
      type: object
      oneOf:
        - required: [create]
        - required: [update]
        - required: [rename]
        - required: [delete]
        - required: [discard]
      properties:
        create:
          $ref: "#/components/schemas/DaemonCreateDraftOperation"
        update:
          $ref: "#/components/schemas/DaemonUpdateDraftOperation"
        rename:
          $ref: "#/components/schemas/DaemonRenameDraftOperation"
        delete:
          $ref: "#/components/schemas/DaemonDeleteDraftOperation"
        discard:
          $ref: "#/components/schemas/DaemonDiscardDraftOperation"
    DaemonCreateDraftOperation:
      type: object
      required: [path, content]
      properties:
        path:
          type: string
          minLength: 1
          description: Portable normalized relative resource path using slash separators.
        content:
          $ref: "#/components/schemas/DaemonDraftContent"
        description:
          type: string
    DaemonUpdateDraftOperation:
      oneOf:
        - $ref: "#/components/schemas/DaemonContentDraftUpdate"
        - $ref: "#/components/schemas/DaemonTextDraftUpdate"
    DaemonContentDraftUpdate:
      type: object
      additionalProperties: false
      required: [id, content]
      properties:
        id:
          type: string
        content:
          $ref: "#/components/schemas/DaemonDraftContent"
        description:
          type: string
    DaemonTextDraftUpdate:
      type: object
      additionalProperties: false
      required: [id, expected_hash, replacements]
      properties:
        id:
          type: string
          minLength: 1
          description: Stable resource ID returned by load_memory, not a path.
        expected_hash:
          type: string
          minLength: 1
          description: Complete Effective Memory content hash returned by load_memory.
        replacements:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/DaemonTextReplacement"
        description:
          type: string
    DaemonTextReplacement:
      type: object
      additionalProperties: false
      required: [old_text, new_text]
      properties:
        old_text:
          type: string
          minLength: 1
        new_text:
          type: string
    DaemonDraftContent:
      type: object
      required: [content]
      properties:
        description:
          type: [string, "null"]
          description: Optional semantic summary updated with the content.
        content:
          type: string
          minLength: 1
    DaemonRenameDraftOperation:
      type: object
      required: [id, new_path]
      properties:
        id:
          type: string
        new_path:
          type: string
          minLength: 1
          description: Portable normalized relative path used by the rename operation.
        description:
          type: string
    DaemonDeleteDraftOperation:
      type: object
      required: [id]
      properties:
        id:
          type: string
        description:
          type: string
    DaemonDiscardDraftOperation:
      type: object
      required: [id]
      properties:
        id:
          type: string
    DaemonDraftOperationResponse:
      type: object
      required: [local_operation_id, draft_id, queued, sync_status]
      properties:
        local_operation_id:
          type: string
        draft_id:
          type: string
        queued:
          type: boolean
        sync_status:
          $ref: "#/components/schemas/DraftOperationSyncStatus"
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/ApiError"
    ApiError:
      type: object
      required: [code, message, request_id]
      properties:
        code:
          type: string
        message:
          type: string
        request_id:
          type: string
        details:
          type: object
          additionalProperties: true
    SyncState:
      type: string
      enum: [idle, queued, syncing, retrying, degraded, failed]
    DaemonDraftResourceKind:
      type: string
      enum: [memory]
    DaemonDraftScope:
      type: string
      enum: [org, project]
    DaemonLocalDraftStatus:
      type: string
      enum: [open, submitted, merged, discarded]
    DaemonDraftFreshness:
      type: string
      enum: [current, behind]
    DaemonDraftReconciliationStatus:
      type: string
      enum: [unknown, clean, conflicts]
    DaemonMemoryCacheState:
      type: string
      enum: [project_ref_not_synced, storage_unavailable, generation_missing, generation_corrupt, ready]
    DaemonProjectStorageMode:
      type: string
      enum: [default, custom]
    DaemonProjectStorageAvailability:
      type: string
      enum: [ready, moving, unavailable]
    DaemonProjectStorageMoveState:
      type: string
      enum: [preparing, materializing, verifying, switching, cleaning, completed, failed]
    ProjectAgentAdapterKind:
      type: string
      enum: [codex, claude-code, opencode, dsh, antigravity]
    AgentRunHost:
      type: string
      enum: [codex, claude-code, zed, manual, opencode, dsh, antigravity]
    AgentRunKind:
      type: string
      enum: [root, subagent]
    AgentRunPhase:
      type: string
      enum: [running, ended]
    AgentRunOutcome:
      type: string
      enum: [completed, blocked, failed, cancelled, unknown]
    AgentRunEventType:
      type: string
      enum: [started, heartbeat, ended, session_ended]
    AgentRunEventSource:
      type: string
      enum: [hook, mcp, desktop, recovery]
    IssueLifecycle:
      type: string
      enum: [open, closed]
    IssueBoardState:
      type: string
      enum: [todo, in_progress, paused, in_review, done]
    IssueExternalReferenceKind:
      type: string
      enum: [issue, pull_request]
    IssueGateAction:
      type: string
      enum: [approve_closure, request_changes, reopen]
    IssueStateEvent:
      type: object
      properties:
        from_state:
          $ref: "#/components/schemas/IssueBoardState"
        to_state:
          $ref: "#/components/schemas/IssueBoardState"
        changed_by_run_id:
          type: string
          nullable: true
        occurred_at:
          type: string
      required: [from_state, to_state, occurred_at]
    VerificationStep:
      type: object
      properties:
        text:
          type: string
        completed:
          type: boolean
          default: false
      required: [text]
    VerificationLevel:
      type: string
      enum: [agent_self, human_required, mixed]
    IssueRemovalAction:
      type: string
      enum: [archive, delete]
    IssueBoardDiagnosticCode:
      type: string
      enum: [malformed_path, malformed_title, title_number_mismatch, duplicate_issue_number]
    RetrievalRunStatus:
      type: string
      enum: [running, succeeded, failed]
    RetrievalExclusionReason:
      type: string
      enum:
        - selected
        - below_relevance
        - overlap
        - per_resource_limit
        - token_budget
        - fragment_limit
        - not_reranked
    RetrievalDeltaAction:
      type: string
      enum: [add, replace, reuse]
    DaemonDraftOperationSource:
      type: string
      enum: [desktop, cli, mcp_store]
    DaemonDraftOperationRecordSource:
      type: string
      enum: [desktop, cli, mcp_store, server]
    DraftOperationSyncStatus:
      type: string
      enum: [queued, syncing, retrying, synced, failed]
