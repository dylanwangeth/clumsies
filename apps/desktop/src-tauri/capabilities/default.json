{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Clumsies desktop windows.",
  "windows": ["main", "authentication"],
  "permissions": [
    "core:default",
    "opener:default",
    "process:allow-restart",
    "updater:default"
  ]
}
