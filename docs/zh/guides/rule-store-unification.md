# 统一 Memory 存储边界迁移

本页是历史迁移记录。Rule、Workflow 与 Context 已统一为 Markdown-backed Organization Memory；Project 只选择资源并承载 Draft overlay。

当前合同以[统一 Memory 模型](/zh/unified-memory-model)为准。旧存储名称、双写和兼容路径不得作为新实现依据。
